import { createClient } from '@supabase/supabase-js'; const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL; const supabaseKey = process.env.SUPABASE_SECRET_KEY; const supabase = createClient(supabaseUrl, supabaseKey); const id = '30538da2-0b90-4422-8017-1bba18ef7a4b'; const { data: inv } = await supabase.from('invitations').select('slug, status').eq('id', id).single(); console.log(inv);
