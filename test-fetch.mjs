import { createClient } from '@supabase/supabase-js'; const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL; const supabaseKey = process.env.SUPABASE_SECRET_KEY; const supabase = createClient(supabaseUrl, supabaseKey); const id = '30538da2-0b90-4422-8017-1bba18ef7a4b'; const { data: inv } = await supabase.from('invitations').select('couple, settings').eq('id', id).single(); const refIds = new Set([inv.couple?.groom?.photoMediaId, inv.couple?.bride?.photoMediaId].filter(v => typeof v === 'string')); const { data: assets } = await supabase.from('media_assets').select('id,status,final_path,kind').eq('invitation_id', id).eq('status', 'ready').in('id', [...refIds]); console.log(JSON.stringify(assets, null, 2));
